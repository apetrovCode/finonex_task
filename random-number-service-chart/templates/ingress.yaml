apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-ingress
spec:
  rules:
  - http:
      paths:
      - path: /number
        pathType: Prefix
        backend:
          service:
            name: random-number
            port:
              number: 80

